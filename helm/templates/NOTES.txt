Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }} in namespace {{ .Values.namespace }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }} -n {{ .Values.namespace }}
  $ helm get all {{ .Release.Name }} -n {{ .Values.namespace }}

{{- if .Values.ingress.enabled }}

Application is accessible via AWS ALB Ingress.

{{- if .Values.service.annotations }}
{{- /* external-dns hostname output removed */ -}}
{{- end }}

Check ingress status:
  $ kubectl get ingress -n {{ .Values.namespace }}
{{- end }}

{{- if contains "LoadBalancer" .Values.service.type }}

Get the LoadBalancer URL:
  $ kubectl get svc {{ include "rbtcfaucet.fullname" . }} -n {{ .Values.namespace }}

NOTE: It may take a few minutes for the LoadBalancer to be available.
{{- end }}

Check deployment status:
  $ kubectl get pods -n {{ .Values.namespace }}
  $ kubectl logs -l app.kubernetes.io/name={{ include "rbtcfaucet.name" . }} -n {{ .Values.namespace }}

{{- if .Values.externalSecrets.enabled }}

External Secrets are enabled. Secrets will be synced from AWS Secrets Manager.
Check external secrets status:
  $ kubectl get externalsecret -n {{ .Values.namespace }}
  $ kubectl get secret {{ include "rbtcfaucet.fullname" . }}-env-secrets -n {{ .Values.namespace }}
{{- else }}

IMPORTANT: Make sure you have created the required secret:
  $ kubectl get secret {{ include "rbtcfaucet.fullname" . }}-env-secrets -n {{ .Values.namespace }}

The secret should contain:
  - REDIS_PASSWORD
  - API_KEY
  - And other application-specific secrets
{{- end }}

{{- if .Values.autoscaling.enabled }}

Horizontal Pod Autoscaler is enabled:
  Min replicas: {{ .Values.autoscaling.minReplicas }}
  Max replicas: {{ .Values.autoscaling.maxReplicas }}
  Target CPU: {{ .Values.autoscaling.targetCPUUtilizationPercentage }}%

Check HPA status:
  $ kubectl get hpa -n {{ .Values.namespace }}
{{- end }}
