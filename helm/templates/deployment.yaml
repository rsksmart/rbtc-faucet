apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "rbtc-faucet.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "rbtc-faucet.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "rbtc-faucet.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "rbtc-faucet.selectorLabels" . | nindent 8 }}
    spec:
      containers:
      - name: {{ .Chart.Name }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.service.targetPort }}
          protocol: TCP
        env:
        - name: PORT
          value: "{{ .Values.service.targetPort }}"
        - name: NODE_ENV
          value: "production"
        {{- range $key, $value := .Values.env }}
        - name: {{ $key }}
          value: {{ $value | quote }}
        {{- end }}
        {{- if and .Values.externalSecrets .Values.externalSecrets.enabled }}
        - name: WS_SECRET
          valueFrom:
            secretKeyRef:
              name: {{ include "rbtc-faucet.fullname" . }}-secrets
              key: ws-secret
        {{- end }}
        livenessProbe:
          {{- toYaml .Values.livenessProbe | nindent 10 }}
        readinessProbe:
          {{- toYaml .Values.readinessProbe | nindent 10 }}
        resources:
          {{- toYaml .Values.resources | nindent 10 }}
